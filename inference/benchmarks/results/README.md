# Benchmark Results

This directory stores CSV output from the Aperture inference benchmarks.

## Files generated:

- `ttft_results.csv` — Time-to-first-token measurements
- `load_test_results.csv` — Per-request latency data across concurrency levels
- `load_test_summary.csv` — P50/P95/P99 summary per concurrency level
